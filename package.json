{
  "name": "@tokenring-ai/workflow",
  "description": "Service for defining and running linear, multi-step agent processes.",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/app": "workspace:*",
    "@tokenring-ai/agent": "workspace:*",
    "@tokenring-ai/rpc": "workspace:*",
    "@tokenring-ai/utility": "workspace:*",
    "zod": "^4.3.6"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "vitest": "^4.1.1",
    "typescript": "^6.0.2"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  }
}
